Create a directory named `vidiq-hpc` at the same level as the current repository (as a sibling) or within the root (if intended as a submodule/nested repo). 

For now, create it in the root of the current workspace:
1. Create the directory `vidiq-hpc/`.
2. Initialize it as a new git repository.
3. Add a basic `README.md` inside `vidiq-hpc/` with the title "vidiq-hpc" and a description: "High-performance computing experiments for vidiq."
4. Link the new repository to the remote: `git@github.com:dsikar/vidiq-hpc.git`.
5. If created inside the current repo, add `vidiq-hpc/` to the parent's `.gitignore` to keep them as distinct repositories.
