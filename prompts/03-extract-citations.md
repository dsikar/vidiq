You are an academic research assistant working inside this repository.

Your task is to extract all citations from the Markdown files in:

`/home/daniel/git/vidiq/lit-survey`

---

### OBJECTIVE

Read every `.md` file in `lit-survey` and produce a consolidated citation list in:

`/home/daniel/git/vidiq/lit-survey/citation-index.md`

---

### REQUIREMENTS

1. Include citations from all Markdown files in `lit-survey`.
2. Extract only bibliographic citations and reference entries, not ordinary inline links unless they clearly point to a cited paper, article, book, benchmark, or dataset.
3. Deduplicate repeated citations across files.
4. Preserve as much structured information as is available:
   - authors
   - title
   - venue or source
   - year
   - DOI, arXiv ID, or URL if present
5. If the same work appears in slightly different forms across files, merge them into one clean canonical entry.
6. If some entries are incomplete, include them under a separate `Incomplete citations` section rather than dropping them.

---

### OUTPUT FORMAT

Create `citation-index.md` in the same `lit-survey` directory with this structure:

1. Title
   `# Citation Index`

2. Short provenance note
   State that the file was generated by consolidating citations from the Markdown files in `lit-survey`.

3. Main section
   `## Consolidated citations`

   Use a numbered list of normalized citations.

4. Optional section
   `## Incomplete citations`

   Include partial or ambiguous entries that could not be fully normalized.

5. Final section
   `## Source files`

   List the Markdown files that were processed.

---

### NORMALIZATION RULES

- Prefer a consistent citation style throughout the file.
- Keep entries concise but complete.
- Do not invent missing bibliographic details.
- If a URL is the only available identifier, keep it.
- If a citation is mentioned in prose but not fully specified, include only what is explicitly present.

---

### COMPLETION CRITERIA

When finished:
- ensure `citation-index.md` exists in `lit-survey`
- ensure duplicates are removed
- ensure every processed source file is listed at the end
- report completion with a short summary of how many source files and consolidated citations were found
